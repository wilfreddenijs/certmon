---
phase: "02-shared-server-mode"
plan: "02-04"
type: "gap_closure"
gap_closure: true
gap_ids:
  - "G-02-1"
  - "G-02-2"
  - "G-02-3"
wave: 4
depends_on:
  - "02-03"
files_modified:
  - ".planning/phases/02-shared-server-mode/02-UAT.md"
  - ".planning/STATE.md"
autonomous: false
requirements: []
must_haves:
  truths:
    - "The published branch contains the form-based authentication UI, Administration user management, and Administration server backup controls already implemented locally."
    - "The release build is traceable to the exact tested Git commit and is not built from stale remote commit e89fa8d."
    - "Focused automated tests pass before publication and browser UAT retests only gaps G-02-1, G-02-2, and G-02-3 against the new build."
  artifacts:
    - "Published Phase 02 branch"
    - "GitHub build tied to an exact commit SHA"
    - "Updated Phase 02 UAT results"
---

# Publish And Verify The Phase 02 Gap-Closure Build

## Objective

Close the delivery gap identified during UAT. The required product changes already exist on the local branch, but the GitHub build tested by the user came from remote commit `e89fa8d`, which predates commits `60f86cf` and `85c57d6`. Verify the local implementation, publish it with explicit approval, build that exact revision, and repeat only the affected browser tests.

<tasks>

<task type="auto">
  <name>Task 1: Verify the local gap-closure implementation and release provenance</name>
  <read_first>
    <file>templates/index.html</file>
    <file>tests/test_ui_contract.py</file>
    <file>tests/test_user_management.py</file>
    <file>tests/test_server_backup_api.py</file>
    <file>.planning/phases/02-shared-server-mode/02-UAT.md</file>
  </read_first>
  <action>
Confirm that the current branch contains native authentication form submission, the Administration user-management section, and the Administration server-backup section. Record the local HEAD and remote branch SHA and fail this task if they are already equal or if the expected commits are absent. Run the focused UI, user-management, and server-backup test suites. Do not change product code merely to satisfy this delivery plan; if a test fails, stop and report that the diagnosis is no longer valid.
  </action>
  <verify>
    <command>py -3 -m pytest tests/test_ui_contract.py tests/test_user_management.py tests/test_server_backup_api.py -q</command>
  </verify>
  <acceptance_criteria>
    <criterion>All focused tests pass.</criterion>
    <criterion>Local HEAD contains commits 60f86cf and 85c57d6 while the previously tested remote revision does not.</criterion>
    <criterion>The worktree contains no uncommitted product-code changes before publication.</criterion>
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Approve publishing the current branch</name>
  <action>
Present the exact branch name, local HEAD, remote SHA, commit count to publish, and focused test result. Ask for explicit approval before pushing. Do not push, merge, delete a branch, or trigger a build until approval is received.
  </action>
  <resume_signal>User explicitly approves the push and build.</resume_signal>
</task>

<task type="auto">
  <name>Task 3: Push and build the exact verified revision</name>
  <read_first>
    <file>.github/workflows</file>
    <file>README.md</file>
  </read_first>
  <action>
Push the current Phase 02 branch without rewriting history. Trigger the repository's existing Windows executable build workflow for that branch, or confirm the push-triggered run. Record the pushed commit SHA, workflow run identifier, build number, and artifact name. Verify that the workflow checkout SHA equals the pushed local HEAD. Do not report a build as ready while it is queued, running, failed, cancelled, or tied to another commit.
  </action>
  <verify>
    <command>git rev-parse HEAD</command>
  </verify>
  <acceptance_criteria>
    <criterion>The remote branch resolves to the same SHA as local HEAD.</criterion>
    <criterion>The GitHub workflow succeeds for that exact SHA.</criterion>
    <criterion>The resulting Windows artifact is available for download and its build description identifies the authentication, Administration, and backup/recovery changes.</criterion>
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Retest the three affected browser workflows</name>
  <action>
Have the user install or run the newly built executable and repeat only UAT tests 4, 5, and 9. Confirm that pressing Enter submits authentication, an admin sees and can use user management, and admin or security_admin sees the server backup and staged recovery controls. Record each result in `02-UAT.md`; do not mark a gap resolved based solely on automated tests.
  </action>
  <resume_signal>User reports pass or describes remaining behavior for UAT tests 4, 5, and 9.</resume_signal>
</task>

</tasks>

## Verification

- The pushed remote branch and successful build workflow reference the same commit SHA as the verified local branch.
- No new product implementation was introduced unless Task 1 invalidated the stale-build diagnosis.
- Gaps `G-02-1`, `G-02-2`, and `G-02-3` are resolved only after browser UAT passes on the new artifact.

